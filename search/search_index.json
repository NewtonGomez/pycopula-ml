{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pycopula-ml documentation","text":"<p>This directory documents the current architecture, mathematical foundations, implemented API, testing strategy, and development roadmap of pycopula-ml.</p> <p>The current statistical workflow has three main stages:</p> \\[ X,Y \\rightarrow U,V \\] <p>through marginal modeling or rank-based pseudo-observations,</p> \\[ (U,V,\\theta) \\rightarrow C_\\theta(u,v) \\] <p>through a bivariate copula, and</p> \\[ (U,V) \\rightarrow \\widehat{\\theta} \\] <p>through numerical parameter estimation.</p> <p>The current implementation includes:</p> <ul> <li>marginal models and marginal model selection;</li> <li>rank-based pseudo-observations;</li> <li>the bivariate Frank copula;</li> <li>copula CDF, density, log-density, and log-likelihood evaluation;</li> <li>scalar maximum-likelihood estimation through <code>fit_copula_mle()</code>;</li> <li>support for disjoint parameter-search intervals;</li> <li>deterministic unit tests and statistical validation workflows.</li> </ul>"},{"location":"#contents","title":"Contents","text":""},{"location":"#getting-started","title":"Getting Started","text":"<ul> <li>Development workflow</li> </ul>"},{"location":"#concepts","title":"Concepts","text":"<ul> <li>Mathematical foundations</li> <li>Copulas</li> <li>Marginal models</li> <li>Parameter estimation</li> </ul>"},{"location":"#architecture","title":"Architecture","text":"<ul> <li>Project architecture</li> </ul>"},{"location":"#copulas","title":"Copulas","text":"<ul> <li>Frank copula</li> </ul>"},{"location":"#marginals","title":"Marginals","text":"<ul> <li>Empirical marginals</li> <li>Parametric marginals</li> <li>Marginal model selection</li> </ul>"},{"location":"#estimation","title":"Estimation","text":"<ul> <li>Maximum-likelihood estimation</li> </ul>"},{"location":"#testing-and-validation","title":"Testing and Validation","text":"<ul> <li>Estimation testing and statistical validation</li> </ul>"},{"location":"#api-reference","title":"API Reference","text":"<ul> <li>Copula API</li> <li>Marginal API</li> <li>Estimation API</li> </ul>"},{"location":"#references","title":"References","text":"<ul> <li>References</li> </ul>"},{"location":"api/copulas/","title":"Copula API","text":""},{"location":"api/copulas/#public-imports","title":"Public imports","text":"<pre><code>from pycopula_ml.copulas import (\n    BivariateCopula,\n    FrankCopula,\n)\n</code></pre>"},{"location":"api/copulas/#bivariatecopula","title":"<code>BivariateCopula</code>","text":"<p>Abstract base class for continuous bivariate copulas.</p>"},{"location":"api/copulas/#cdfu-v","title":"<code>cdf(u, v)</code>","text":"<p>Evaluate</p> \\[ C(u,v). \\] <p>Subclasses must implement this method.</p>"},{"location":"api/copulas/#pdfu-v","title":"<code>pdf(u, v)</code>","text":"<p>Evaluate</p> \\[ c(u,v) = \\frac{\\partial^2C(u,v)} {\\partial u\\,\\partial v}. \\] <p>Subclasses must implement this method.</p>"},{"location":"api/copulas/#logpdfu-v","title":"<code>logpdf(u, v)</code>","text":"<p>Evaluate</p> \\[ \\log c(u,v). \\] <p>The base implementation may use <code>np.log(pdf())</code>.</p> <p>A subclass can override it with a numerically stable analytical expression.</p>"},{"location":"api/copulas/#log_likelihoodu-v","title":"<code>log_likelihood(u, v)</code>","text":"<p>Evaluate the sample copula log-likelihood</p> \\[ \\ell = \\sum_i\\log c(u_i,v_i). \\] <p>This method evaluates the likelihood at the parameter already stored in the copula instance.</p> <p>Parameter search itself belongs to <code>pycopula_ml.estimation</code>.</p>"},{"location":"api/copulas/#_prepare_inputsu-v","title":"<code>_prepare_inputs(u, v)</code>","text":"<p>Internal helper.</p> <p>Responsibilities:</p> <ul> <li>convert to NumPy arrays;</li> <li>enforce floating-point representation;</li> <li>broadcast compatible inputs;</li> <li>reject non-finite values;</li> <li>enforce \\([0,1]\\) domain.</li> </ul>"},{"location":"api/copulas/#frankcopula","title":"<code>FrankCopula</code>","text":""},{"location":"api/copulas/#constructor","title":"Constructor","text":"<pre><code>FrankCopula(theta=2.0)\n</code></pre>"},{"location":"api/copulas/#parameter","title":"Parameter","text":"<pre><code>theta : float\n</code></pre> <p>Requirements:</p> <ul> <li>finite;</li> <li>non-zero.</li> </ul>"},{"location":"api/copulas/#methods","title":"Methods","text":"<pre><code>cdf(u, v)\npdf(u, v)\nlogpdf(u, v)\nlog_likelihood(u, v)\n</code></pre>"},{"location":"api/copulas/#example-known-parameter","title":"Example: known parameter","text":"<pre><code>import numpy as np\n\nfrom pycopula_ml.copulas import FrankCopula\n\n\nu = np.array([0.2, 0.4, 0.7])\nv = np.array([0.3, 0.6, 0.8])\n\ncopula = FrankCopula(theta=2.0)\n\nprint(copula.cdf(u, v))\nprint(copula.pdf(u, v))\nprint(copula.logpdf(u, v))\nprint(copula.log_likelihood(u, v))\n</code></pre>"},{"location":"api/copulas/#example-unknown-parameter","title":"Example: unknown parameter","text":"<p>Use the estimation API rather than placing optimization logic inside the copula class:</p> <pre><code>from pycopula_ml.copulas import FrankCopula\nfrom pycopula_ml.estimation import fit_copula_mle\n\n\nresult = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n\ncopula = FrankCopula(theta=result.theta)\n</code></pre> <p>See Estimation API.</p>"},{"location":"api/estimation/","title":"Estimation API","text":""},{"location":"api/estimation/#public-imports","title":"Public imports","text":"<pre><code>from pycopula_ml.estimation import (\n    CopulaFitResult,\n    fit_copula_mle,\n)\n</code></pre>"},{"location":"api/estimation/#fit_copula_mle","title":"<code>fit_copula_mle</code>","text":"<p>Estimate a scalar copula parameter by maximizing the sample copula log-likelihood.</p> <p>Conceptual signature:</p> <pre><code>fit_copula_mle(\n    copula,\n    u,\n    v,\n    bounds,\n    *,\n    xatol=1e-8,\n    maxiter=500,\n) -&gt; CopulaFitResult\n</code></pre>"},{"location":"api/estimation/#parameters","title":"Parameters","text":""},{"location":"api/estimation/#copula","title":"<code>copula</code>","text":"<p>Copula class or factory accepting</p> <pre><code>theta=&lt;float&gt;\n</code></pre> <p>and returning an object that implements</p> <pre><code>log_likelihood(u, v)\n</code></pre> <p>Example:</p> <pre><code>FrankCopula\n</code></pre> <p>Pass the class, not an already instantiated object, because the optimizer must construct candidate copulas for many values of <code>theta</code>.</p>"},{"location":"api/estimation/#u-v","title":"<code>u</code>, <code>v</code>","text":"<p>Paired pseudo-observations.</p> <p>Requirements:</p> <pre><code>one-dimensional\nsame shape\nnon-empty\nfinite\nstrictly inside (0, 1)\n</code></pre>"},{"location":"api/estimation/#bounds","title":"<code>bounds</code>","text":"<p>Either one interval:</p> <pre><code>bounds=(1e-6, 50.0)\n</code></pre> <p>or several intervals:</p> <pre><code>bounds=(\n    (-50.0, -1e-6),\n    (1e-6, 50.0),\n)\n</code></pre> <p>Every interval must satisfy</p> \\[ \\text{lower}&lt;\\text{upper}. \\]"},{"location":"api/estimation/#xatol","title":"<code>xatol</code>","text":"<p>Absolute scalar-parameter tolerance used by the bounded numerical optimizer.</p> <p>Default:</p> <pre><code>1e-8\n</code></pre>"},{"location":"api/estimation/#maxiter","title":"<code>maxiter</code>","text":"<p>Maximum iterations per interval.</p> <p>Default:</p> <pre><code>500\n</code></pre>"},{"location":"api/estimation/#returns","title":"Returns","text":"<p>A <code>CopulaFitResult</code>.</p>"},{"location":"api/estimation/#raises","title":"Raises","text":"<p><code>ValueError</code> for invalid pseudo-observations, bounds, <code>xatol</code>, or <code>maxiter</code>.</p> <p><code>RuntimeError</code> if all searched intervals fail to produce a finite successful optimization.</p>"},{"location":"api/estimation/#copulafitresult","title":"<code>CopulaFitResult</code>","text":"<p>Structured result returned by <code>fit_copula_mle()</code>.</p> <p>Fields:</p> <pre><code>theta\nlog_likelihood\nsuccess\nmessage\nnfev\nn_obs\ninterval\nraw_result\ninterval_results\n</code></pre>"},{"location":"api/estimation/#theta","title":"<code>theta</code>","text":"<p>Estimated dependence parameter.</p>"},{"location":"api/estimation/#log_likelihood","title":"<code>log_likelihood</code>","text":"<p>Maximized copula log-likelihood:</p> \\[ \\ell(\\widehat{\\theta}). \\]"},{"location":"api/estimation/#success","title":"<code>success</code>","text":"<p>Whether the selected SciPy optimization converged successfully.</p>"},{"location":"api/estimation/#message","title":"<code>message</code>","text":"<p>Optimizer convergence message.</p>"},{"location":"api/estimation/#nfev","title":"<code>nfev</code>","text":"<p>Number of objective-function evaluations for the selected interval.</p>"},{"location":"api/estimation/#n_obs","title":"<code>n_obs</code>","text":"<p>Number of paired pseudo-observations used in the fit.</p>"},{"location":"api/estimation/#interval","title":"<code>interval</code>","text":"<p>Search interval that produced the selected optimum.</p>"},{"location":"api/estimation/#raw_result","title":"<code>raw_result</code>","text":"<p>Original SciPy <code>OptimizeResult</code> associated with the selected interval.</p> <p>This field is primarily for diagnostics.</p>"},{"location":"api/estimation/#interval_results","title":"<code>interval_results</code>","text":"<p>Tuple containing the raw SciPy result for every searched interval.</p>"},{"location":"api/estimation/#example","title":"Example","text":"<pre><code>from pycopula_ml.copulas import FrankCopula\nfrom pycopula_ml.estimation import fit_copula_mle\nfrom pycopula_ml.marginals import bivariate_pseudo_observations\n\n\nu, v = bivariate_pseudo_observations(x, y)\n\nresult = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n\nprint(f\"theta_hat: {result.theta:.8f}\")\nprint(f\"log-likelihood: {result.log_likelihood:.8f}\")\nprint(f\"observations: {result.n_obs}\")\nprint(f\"selected interval: {result.interval}\")\n</code></pre>"},{"location":"api/marginals/","title":"Marginal API","text":""},{"location":"api/marginals/#public-imports","title":"Public imports","text":"<pre><code>from pycopula_ml.marginals import (\n    ContinuousMarginalModel,\n    EmpiricalMarginal,\n    MarginalCandidate,\n    MarginalFitResult,\n    MarginalModel,\n    MarginalSelector,\n    ParametricMarginal,\n    bivariate_pseudo_observations,\n    pseudo_observations,\n)\n</code></pre>"},{"location":"api/marginals/#marginalmodel","title":"<code>MarginalModel</code>","text":"<p>Abstract interface.</p>"},{"location":"api/marginals/#fitvalues","title":"<code>fit(values)</code>","text":"<p>Fit the model.</p>"},{"location":"api/marginals/#cdfvalues","title":"<code>cdf(values)</code>","text":"<p>Evaluate the cumulative distribution function.</p>"},{"location":"api/marginals/#transformvalues-epsilon1e-10","title":"<code>transform(values, epsilon=1e-10)</code>","text":"<p>Compute</p> \\[ u_i=F_X(x_i) \\] <p>and clip into the open unit interval.</p>"},{"location":"api/marginals/#continuousmarginalmodel","title":"<code>ContinuousMarginalModel</code>","text":"<p>Extends <code>MarginalModel</code>.</p> <p>Requires:</p> <pre><code>pdf(values)\nlogpdf(values)\n</code></pre>"},{"location":"api/marginals/#parametricmarginal","title":"<code>ParametricMarginal</code>","text":"<p>Generic wrapper around a continuous SciPy distribution.</p> <p>Constructor:</p> <pre><code>ParametricMarginal(\n    distribution,\n    name=None,\n    fit_kwargs=None,\n)\n</code></pre> <p>Important attributes:</p> <pre><code>parameters_\nresult_\nloc_\nscale_\n</code></pre> <p>Methods:</p> <pre><code>fit(values)\npdf(values)\nlogpdf(values)\ncdf(values)\ntransform(values)\nsupport()\n</code></pre>"},{"location":"api/marginals/#marginalfitresult","title":"<code>MarginalFitResult</code>","text":"<p>Structured fit diagnostics.</p> <p>Fields:</p> <pre><code>distribution\nparameters\nsample_size\nn_parameters\nlog_likelihood\naic\nbic\nks_statistic\nks_pvalue\n</code></pre>"},{"location":"api/marginals/#marginalcandidate","title":"<code>MarginalCandidate</code>","text":"<p>Defines a distribution considered by <code>MarginalSelector</code>.</p> <p>Example:</p> <pre><code>from scipy.stats import gamma\n\ncandidate = MarginalCandidate(\n    name=\"gamma\",\n    distribution=gamma,\n    fit_kwargs={\"floc\": 0.0},\n)\n</code></pre>"},{"location":"api/marginals/#marginalselector","title":"<code>MarginalSelector</code>","text":"<p>Constructor:</p> <pre><code>MarginalSelector(\n    candidates=None,\n    criterion=\"aic\",\n    small_sample_warning=20,\n    boundary_tolerance=1e-6,\n)\n</code></pre> <p>Important attributes:</p> <pre><code>selected_model_\nselected_result_\nresults_\nfailed_\nwarnings_\n</code></pre> <p>Convenience aliases:</p> <pre><code>best_model_\nbest_result_\n</code></pre> <p>Methods:</p> <pre><code>fit(values)\ntransform(values)\nfit_transform(values)\nranking()\nsummary()\n</code></pre>"},{"location":"api/marginals/#empiricalmarginal","title":"<code>EmpiricalMarginal</code>","text":"<p>Implements the empirical CDF.</p> <p>Methods:</p> <pre><code>fit(values)\ncdf(values)\ntransform(values)\nfit_transform(values)\n</code></pre>"},{"location":"api/marginals/#pseudo_observationsvalues-tiesaverage","title":"<code>pseudo_observations(values, ties=\"average\")</code>","text":"<p>Computes</p> \\[ u_i=\\frac{R_i}{n+1}. \\]"},{"location":"api/marginals/#bivariate_pseudo_observationsx-y-tiesaverage","title":"<code>bivariate_pseudo_observations(x, y, ties=\"average\")</code>","text":"<p>Returns:</p> <pre><code>u, v\n</code></pre> <p>for two paired samples.</p> <p>The two samples must have the same number of observations.</p>"},{"location":"architecture/architecture/","title":"Architecture","text":""},{"location":"architecture/architecture/#objective","title":"Objective","text":"<p>The architecture separates four statistical responsibilities:</p> <ol> <li>marginal modeling;</li> <li>dependence modeling with copulas;</li> <li>parameter estimation;</li> <li>future classification layers.</li> </ol> <p>The current package layout is:</p> <pre><code>src/pycopula_ml/\n\u251c\u2500\u2500 copulas/\n\u2502   \u251c\u2500\u2500 base.py\n\u2502   \u2514\u2500\u2500 frank.py\n\u251c\u2500\u2500 estimation/\n\u2502   \u2514\u2500\u2500 maximum_likelihood.py\n\u2514\u2500\u2500 marginals/\n    \u251c\u2500\u2500 base.py\n    \u251c\u2500\u2500 empirical.py\n    \u251c\u2500\u2500 parametric.py\n    \u251c\u2500\u2500 result.py\n    \u2514\u2500\u2500 selection.py\n</code></pre>"},{"location":"architecture/architecture/#statistical-flow","title":"Statistical flow","text":"<p>For two original variables \\(X\\) and \\(Y\\),</p> \\[ X \\rightarrow \\widehat F_X \\rightarrow U, \\] \\[ Y \\rightarrow \\widehat F_Y \\rightarrow V. \\] <p>The dependence model then evaluates</p> \\[ (U,V,\\theta) \\rightarrow C_\\theta(U,V). \\] <p>If \\(\\theta\\) is unknown, the estimation layer uses the sample log-likelihood</p> \\[ \\ell(\\theta) = \\sum_{i=1}^{n} \\log c_\\theta(u_i,v_i) \\] <p>to obtain</p> \\[ \\widehat{\\theta} = \\operatorname*{argmax}_{\\theta} \\ell(\\theta). \\] <p>The full fitting workflow is therefore</p> <pre><code>original data\n    \u2193\nmarginal transformation\n    \u2193\npseudo-observations (u, v)\n    \u2193\ncopula log-likelihood\n    \u2193\nparameter estimation\n    \u2193\ntheta_hat\n</code></pre> <p>This separation is deliberate.</p> <p>A copula should not need to know whether \\(U\\) and \\(V\\) came from:</p> <ul> <li>a Normal marginal;</li> <li>a Gamma marginal;</li> <li>a Lognormal marginal;</li> <li>an empirical CDF;</li> <li>rank-based pseudo-observations.</li> </ul> <p>Likewise, the estimation layer should not contain the analytical density formula for every copula family. It operates through the common copula interface.</p>"},{"location":"architecture/architecture/#copula-hierarchy","title":"Copula hierarchy","text":"<pre><code>BivariateCopula\n\u2514\u2500\u2500 FrankCopula\n</code></pre> <p><code>BivariateCopula</code> defines common operations:</p> <ul> <li><code>cdf()</code></li> <li><code>pdf()</code></li> <li><code>logpdf()</code></li> <li><code>log_likelihood()</code></li> <li>common input preparation and validation</li> </ul> <p><code>FrankCopula</code> provides the Frank-specific formulas and parameter validation.</p> <p>Future families can derive from the same interface.</p>"},{"location":"architecture/architecture/#marginal-hierarchy","title":"Marginal hierarchy","text":"<pre><code>MarginalModel\n\u251c\u2500\u2500 EmpiricalMarginal\n\u2514\u2500\u2500 ContinuousMarginalModel\n    \u2514\u2500\u2500 ParametricMarginal\n</code></pre> <p><code>MarginalModel</code> defines:</p> <ul> <li><code>fit()</code></li> <li><code>cdf()</code></li> <li><code>transform()</code></li> </ul> <p><code>ContinuousMarginalModel</code> additionally requires:</p> <ul> <li><code>pdf()</code></li> <li><code>logpdf()</code></li> </ul> <p>This distinction matters because an empirical CDF is a step function and does not have an ordinary continuous density suitable for the same PDF interface.</p>"},{"location":"architecture/architecture/#model-selection-layer","title":"Model-selection layer","text":"<pre><code>MarginalSelector\n\u251c\u2500\u2500 MarginalCandidate\n\u251c\u2500\u2500 ParametricMarginal\n\u2514\u2500\u2500 MarginalFitResult\n</code></pre> <p><code>MarginalSelector</code> is responsible for:</p> <ul> <li>fitting multiple candidate distributions;</li> <li>recording failed candidates;</li> <li>ranking successful candidates;</li> <li>selecting by AIC, BIC, or KS statistic;</li> <li>producing warnings;</li> <li>transforming observations with the selected marginal.</li> </ul>"},{"location":"architecture/architecture/#estimation-layer","title":"Estimation layer","text":"<pre><code>fit_copula_mle()\n\u2514\u2500\u2500 CopulaFitResult\n</code></pre> <p>The estimation layer is responsible for:</p> <ul> <li>validating pseudo-observations for likelihood evaluation;</li> <li>evaluating candidate copula log-likelihoods;</li> <li>minimizing the negative log-likelihood;</li> <li>searching one or several bounded parameter intervals;</li> <li>selecting the best successful solution;</li> <li>returning statistical and numerical diagnostics.</li> </ul> <p>It is intentionally generic with respect to the copula family.</p> <p>The estimator requires a copula class or factory that can be called as</p> <pre><code>copula(theta=&lt;candidate&gt;)\n</code></pre> <p>and whose instances implement</p> <pre><code>log_likelihood(u, v)\n</code></pre> <p>This is why user code passes</p> <pre><code>FrankCopula\n</code></pre> <p>rather than an already instantiated</p> <pre><code>FrankCopula(theta=...)\n</code></pre> <p>to <code>fit_copula_mle()</code>.</p>"},{"location":"architecture/architecture/#why-estimation-is-not-under-tools","title":"Why estimation is not under <code>tools</code>","text":"<p>A generic package path such as</p> <pre><code>pycopula_ml.tools\n</code></pre> <p>does not communicate the statistical responsibility of the code.</p> <p>Maximum-likelihood fitting belongs under</p> <pre><code>pycopula_ml.estimation\n</code></pre> <p>because it is part of the public statistical model-fitting API, not merely an internal utility.</p> <p>This also leaves room for future estimators such as:</p> <pre><code>estimation/\n\u251c\u2500\u2500 maximum_likelihood.py\n\u251c\u2500\u2500 rank_based.py\n\u2514\u2500\u2500 ...\n</code></pre> <p>without mixing statistical estimation with unrelated helpers.</p>"},{"location":"architecture/architecture/#design-rule","title":"Design rule","text":"<p>The classifier planned for later should not contain logic such as:</p> <pre><code>if family == \"frank\":\n    ...\nelif family == \"clayton\":\n    ...\n</code></pre> <p>It should operate through a common copula interface.</p> <p>Similarly:</p> <ul> <li>the copula layer should not decide which marginal distribution produced   \\(U\\) and \\(V\\);</li> <li>the estimation layer should not reimplement family-specific copula   mathematics;</li> <li>user scripts should not depend directly on the SciPy optimization backend.</li> </ul>"},{"location":"architecture/architecture/#separation-of-calculation-and-presentation","title":"Separation of calculation and presentation","text":"<p>Core statistical classes and functions return values and structured results.</p> <p>They should not depend on:</p> <ul> <li><code>rich</code>;</li> <li>terminal formatting;</li> <li>plotting;</li> <li>interactive input.</li> </ul> <p>Presentation utilities can be added separately later.</p> <p>This keeps the statistical core usable from scripts, notebooks, tests, services, and future classifier components.</p>"},{"location":"concepts/copulas/","title":"Copulas","text":""},{"location":"concepts/copulas/#purpose","title":"Purpose","text":"<p>The <code>pycopula_ml.copulas</code> package models dependence after marginal variables have been transformed to the unit interval.</p> <p>For CDF and density evaluation, copula inputs belong to</p> \\[ u,v\\in[0,1]. \\] <p>For parameter estimation through log-likelihood, the current estimation layer requires pseudo-observations strictly inside</p> \\[ 0&lt;u,v&lt;1. \\] <p>Original measurements \\(X,Y\\) should not be passed directly to a copula unless they are already uniform.</p>"},{"location":"concepts/copulas/#public-hierarchy","title":"Public hierarchy","text":"<pre><code>BivariateCopula\n\u2514\u2500\u2500 FrankCopula\n</code></pre>"},{"location":"concepts/copulas/#bivariatecopula","title":"<code>BivariateCopula</code>","text":"<p>The abstract base class defines a common interface for continuous bivariate copulas.</p> <p>Required subclass implementations:</p> <pre><code>cdf(u, v)\npdf(u, v)\n</code></pre> <p>Default/common functionality:</p> <pre><code>logpdf(u, v)\nlog_likelihood(u, v)\n</code></pre> <p>The default <code>logpdf()</code> can be computed as</p> <pre><code>np.log(self.pdf(u, v))\n</code></pre> <p>but subclasses may override it with a direct analytical expression for numerical stability.</p>"},{"location":"concepts/copulas/#common-input-preparation","title":"Common input preparation","text":"<p><code>BivariateCopula._prepare_inputs()</code> performs common validation before family-specific mathematics is evaluated.</p> <p>It:</p> <ol> <li>converts inputs to NumPy floating-point arrays;</li> <li>broadcasts compatible shapes;</li> <li>rejects non-finite values;</li> <li>verifies that all values belong to \\([0,1]\\).</li> </ol> <p>This logic belongs in the base class because it applies to all copula families.</p> <p>Family-specific parameter restrictions remain in each subclass.</p>"},{"location":"concepts/copulas/#sample-log-likelihood","title":"Sample log-likelihood","text":"<p>For a copula parameterized by \\(\\theta\\), the generic sample log-likelihood is</p> \\[ \\ell(\\theta) = \\sum_i \\log c_\\theta(u_i,v_i). \\] <p>The density itself changes by family; the likelihood construction does not.</p> <p>This common method is the interface used by the estimation layer:</p> <pre><code>fit_copula_mle()\n        \u2193\ncopula(theta=candidate)\n        \u2193\nlog_likelihood(u, v)\n</code></pre> <p>The copula class therefore contains the probability model, while <code>pycopula_ml.estimation</code> contains the numerical fitting strategy.</p>"},{"location":"concepts/copulas/#current-family-support","title":"Current family support","text":"<p>Implemented:</p> <ul> <li>Frank</li> </ul> <p>Planned:</p> <ul> <li>Clayton</li> <li>Gumbel</li> <li>Joe</li> <li>AMH</li> <li>Gaussian</li> <li>Independence</li> <li>later Student-t and additional families</li> </ul>"},{"location":"concepts/copulas/#parameter-estimation","title":"Parameter estimation","text":"<p>Copula objects are instantiated with known parameters:</p> <pre><code>copula = FrankCopula(theta=2.0)\n</code></pre> <p>When the parameter is unknown, use the estimation package instead:</p> <pre><code>from pycopula_ml.estimation import fit_copula_mle\n\nresult = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n\ntheta_hat = result.theta\n</code></pre> <p>This avoids placing optimizer-specific logic inside <code>FrankCopula</code>.</p>"},{"location":"concepts/estimation/","title":"Parameter estimation","text":""},{"location":"concepts/estimation/#purpose","title":"Purpose","text":"<p>The <code>pycopula_ml.estimation</code> package estimates copula parameters after the marginal variables have been transformed to pseudo-observations.</p> <p>The current workflow is</p> \\[ (X,Y) \\rightarrow (U,V) \\rightarrow \\widehat{\\theta}. \\] <p>The responsibilities are deliberately separated:</p> <ul> <li><code>pycopula_ml.marginals</code> transforms original measurements into values on the   unit interval;</li> <li><code>pycopula_ml.copulas</code> evaluates a copula for a supplied parameter;</li> <li><code>pycopula_ml.estimation</code> searches for the parameter that best explains the   observed dependence under the selected copula family.</li> </ul>"},{"location":"concepts/estimation/#maximum-likelihood-principle","title":"Maximum-likelihood principle","text":"<p>Given paired pseudo-observations</p> \\[ (u_1,v_1),\\ldots,(u_n,v_n), \\] <p>and a copula density \\(c_\\theta\\), the sample copula log-likelihood is</p> \\[ \\ell(\\theta) = \\sum_{i=1}^{n} \\log c_\\theta(u_i,v_i). \\] <p>The estimator seeks</p> \\[ \\widehat{\\theta} = \\operatorname*{argmax}_{\\theta} \\ell(\\theta). \\] <p>The implementation uses a numerical minimizer, so it minimizes the equivalent objective</p> \\[ Q(\\theta) = -\\ell(\\theta). \\] <p>Therefore,</p> \\[ \\operatorname*{argmin}_{\\theta} Q(\\theta) = \\operatorname*{argmax}_{\\theta} \\ell(\\theta). \\]"},{"location":"concepts/estimation/#why-the-optimizer-is-outside-the-copula-classes","title":"Why the optimizer is outside the copula classes","text":"<p>A copula object answers questions such as:</p> <pre><code>copula.cdf(u, v)\ncopula.pdf(u, v)\ncopula.logpdf(u, v)\ncopula.log_likelihood(u, v)\n</code></pre> <p>Those operations assume that <code>theta</code> is already known.</p> <p>Parameter estimation is a different responsibility. It repeatedly creates candidate copulas, evaluates their log-likelihoods, and compares the resulting objective values.</p> <p>Keeping these responsibilities separate avoids embedding optimization policy inside each copula family.</p>"},{"location":"concepts/estimation/#pseudo-observations","title":"Pseudo-observations","text":"<p>The estimator expects paired one-dimensional arrays satisfying</p> \\[ 0&lt;u_i&lt;1, \\qquad 0&lt;v_i&lt;1. \\] <p>The open interval is intentional. Exact values of \\(0\\) or \\(1\\) can create singular or numerically unstable log-density evaluations for several copula families.</p> <p>Original measurements should therefore first be transformed through either:</p> <ul> <li>fitted marginal CDFs; or</li> <li>rank-based pseudo-observations.</li> </ul> <p>For example:</p> <pre><code>from pycopula_ml.marginals import bivariate_pseudo_observations\n\nu, v = bivariate_pseudo_observations(x, y)\n</code></pre>"},{"location":"concepts/estimation/#likelihood-vs-pseudo-likelihood","title":"Likelihood vs pseudo-likelihood","text":"<p>When \\(u_i\\) and \\(v_i\\) are observed uniforms under known marginals, maximizing the copula log-likelihood is ordinary maximum-likelihood estimation for the copula parameter.</p> <p>When \\(u_i\\) and \\(v_i\\) are estimated from the same sample, for example by ranks,</p> \\[ u_i=\\frac{R_i}{n+1}, \\qquad v_i=\\frac{S_i}{n+1}, \\] <p>the procedure is commonly described more precisely as a copula pseudo-likelihood or maximum pseudo-likelihood approach.</p> <p>The current <code>fit_copula_mle()</code> API maximizes the supplied copula log-likelihood in either case. It does not attempt to infer how the pseudo-observations were constructed.</p>"},{"location":"concepts/estimation/#disjoint-parameter-domains","title":"Disjoint parameter domains","text":"<p>Some copula families have parameter spaces that are not represented by one closed numerical interval.</p> <p>For the Frank family,</p> \\[ \\theta\\in\\mathbb R\\setminus\\{0\\}, \\] <p>while independence is obtained only in the limit</p> \\[ \\theta\\rightarrow0. \\] <p>Because <code>FrankCopula(theta=0)</code> is not part of the direct parameterization, the estimator can search both sides of zero separately:</p> <pre><code>result = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n</code></pre> <p>Each interval is optimized independently and the solution with the largest log-likelihood is selected.</p>"},{"location":"concepts/estimation/#current-scope","title":"Current scope","text":"<p>The estimation layer currently supports:</p> <ul> <li>one scalar dependence parameter;</li> <li>bounded one-dimensional numerical optimization;</li> <li>one interval or several disjoint intervals;</li> <li>structured optimization results;</li> <li>validation of pseudo-observations and bounds.</li> </ul> <p>It does not yet provide:</p> <ul> <li>standard errors or confidence intervals;</li> <li>Hessian-based uncertainty estimates;</li> <li>multivariate parameter optimization;</li> <li>automatic copula-family selection;</li> <li>automatic parameter-domain discovery from a copula class;</li> <li>rank-inversion estimators such as inversion of Kendall's tau.</li> </ul>"},{"location":"concepts/marginals/","title":"Marginal models","text":""},{"location":"concepts/marginals/#why-marginals-are-required","title":"Why marginals are required","text":"<p>Copulas operate on uniform variables, not directly on arbitrary measurements.</p> <p>Given data \\(X\\),</p> \\[ X \\rightarrow F_X \\rightarrow U=F_X(X). \\] <p>The <code>marginals</code> package provides two strategies:</p> <ol> <li>parametric marginal estimation;</li> <li>empirical/rank-based transformation.</li> </ol>"},{"location":"concepts/marginals/#public-hierarchy","title":"Public hierarchy","text":"<pre><code>MarginalModel\n\u251c\u2500\u2500 EmpiricalMarginal\n\u2514\u2500\u2500 ContinuousMarginalModel\n    \u2514\u2500\u2500 ParametricMarginal\n</code></pre>"},{"location":"concepts/marginals/#marginalmodel","title":"<code>MarginalModel</code>","text":"<p>Common operations:</p> <ul> <li><code>fit(values)</code></li> <li><code>cdf(values)</code></li> <li><code>transform(values)</code></li> </ul> <p>The generic transform applies</p> \\[ u_i=F_X(x_i) \\] <p>and clips exact boundary values to avoid numerical problems in later copula calculations.</p>"},{"location":"concepts/marginals/#continuousmarginalmodel","title":"<code>ContinuousMarginalModel</code>","text":"<p>Adds:</p> <ul> <li><code>pdf(values)</code></li> <li><code>logpdf(values)</code></li> </ul> <p>This interface is appropriate for continuous parametric distributions.</p>"},{"location":"concepts/marginals/#why-empirical-marginals-are-separate","title":"Why empirical marginals are separate","text":"<p>An empirical CDF is a step function.</p> <p>It does not have an ordinary continuous density equivalent to the PDFs provided by SciPy continuous distributions.</p> <p>Therefore <code>EmpiricalMarginal</code> derives from <code>MarginalModel</code>, not <code>ContinuousMarginalModel</code>.</p>"},{"location":"concepts/marginals/#parametric-path","title":"Parametric path","text":"\\[ X \\rightarrow \\widehat F_X^{\\text{parametric}} \\rightarrow U. \\] <p>Example:</p> <pre><code>from pycopula_ml.marginals import MarginalSelector\n\nselector = MarginalSelector().fit(x)\nu = selector.transform(x)\n</code></pre>"},{"location":"concepts/marginals/#rank-based-path","title":"Rank-based path","text":"\\[ X \\rightarrow R_X \\rightarrow U=\\frac{R_X}{n+1}. \\] <p>Example:</p> <pre><code>from pycopula_ml.marginals import pseudo_observations\n\nu = pseudo_observations(x)\n</code></pre> <p>The rank-based route avoids assuming a theoretical marginal family.</p>"},{"location":"concepts/mathematical_foundations/","title":"Mathematical foundations","text":""},{"location":"concepts/mathematical_foundations/#marginal-distributions","title":"Marginal distributions","text":"<p>For a random variable \\(X\\), its cumulative distribution function is</p> \\[ F_X(x)=P(X\\leq x). \\] <p>For a continuous distribution, its density is</p> \\[ f_X(x)=\\frac{dF_X(x)}{dx}. \\] <p>A probability over an interval is obtained by integration:</p> \\[ P(a&lt;X&lt;b) = \\int_a^b f_X(x)\\,dx. \\]"},{"location":"concepts/mathematical_foundations/#bivariate-distribution","title":"Bivariate distribution","text":"<p>For two random variables \\(X\\) and \\(Y\\),</p> \\[ F_{X,Y}(x,y) = P(X\\leq x,\\;Y\\leq y). \\] <p>If the joint distribution is continuous,</p> \\[ f_{X,Y}(x,y) = \\frac{\\partial^2F_{X,Y}(x,y)} {\\partial x\\,\\partial y}. \\]"},{"location":"concepts/mathematical_foundations/#probability-integral-transform","title":"Probability integral transform","text":"<p>For a continuous variable \\(X\\),</p> \\[ U=F_X(X) \\] <p>is Uniform on \\((0,1)\\) under the ideal population model.</p> <p>Similarly,</p> \\[ V=F_Y(Y). \\] <p>This transformation is the bridge between real-world variables and copulas.</p>"},{"location":"concepts/mathematical_foundations/#sklars-theorem","title":"Sklar's theorem","text":"<p>For continuous marginal distributions,</p> \\[ F_{X,Y}(x,y) = C(F_X(x),F_Y(y)), \\] <p>or</p> \\[ F_{X,Y}(x,y) = C(u,v). \\] <p>At the density level,</p> \\[ f_{X,Y}(x,y) = c(u,v) f_X(x) f_Y(y). \\] <p>This separates:</p> <ul> <li>marginal behavior \\(f_X,f_Y\\);</li> <li>dependence behavior \\(c\\).</li> </ul>"},{"location":"concepts/mathematical_foundations/#copula-cdf","title":"Copula CDF","text":"<p>A bivariate copula is a CDF on the unit square:</p> \\[ C(u,v) = P(U\\leq u,\\;V\\leq v). \\] <p>It satisfies properties including</p> \\[ C(u,0)=0, \\] \\[ C(0,v)=0, \\] \\[ C(u,1)=u, \\] \\[ C(1,v)=v. \\]"},{"location":"concepts/mathematical_foundations/#copula-density","title":"Copula density","text":"<p>For a continuous bivariate copula,</p> \\[ c(u,v) = \\frac{\\partial^2C(u,v)} {\\partial u\\,\\partial v}. \\] <p>This is what the package names <code>pdf()</code>.</p>"},{"location":"concepts/mathematical_foundations/#log-density","title":"Log-density","text":"<p>The log-density is</p> \\[ \\log c(u,v). \\] <p>It is not a different distribution. It is the same density represented on a logarithmic scale.</p> <p>The package names this operation <code>logpdf()</code>.</p>"},{"location":"concepts/mathematical_foundations/#likelihood","title":"Likelihood","text":"<p>Given paired observations</p> \\[ (u_1,v_1),\\ldots,(u_n,v_n), \\] <p>the copula likelihood for a parameter \\(\\theta\\) is</p> \\[ L(\\theta) = \\prod_{i=1}^n c_\\theta(u_i,v_i). \\] <p>The log-likelihood is</p> \\[ \\ell(\\theta) = \\sum_{i=1}^n \\log c_\\theta(u_i,v_i). \\] <p>This form is preferred computationally because it converts products into sums and reduces numerical underflow.</p> <p>In the package:</p> <pre><code>copula.log_likelihood(u, v)\n</code></pre> <p>returns this summed log-density for the current copula parameter.</p>"},{"location":"concepts/mathematical_foundations/#maximum-likelihood-estimation","title":"Maximum-likelihood estimation","text":"<p>The current scalar estimator solves</p> \\[ \\widehat\\theta = \\operatorname*{argmax}_\\theta \\ell(\\theta). \\] <p>The implementation uses a numerical minimizer on the equivalent objective</p> \\[ Q(\\theta) = -\\ell(\\theta). \\] <p>Therefore,</p> \\[ \\widehat\\theta = \\operatorname*{argmin}_\\theta [-\\ell(\\theta)]. \\] <p>In code:</p> <pre><code>from pycopula_ml.estimation import fit_copula_mle\n\nresult = fit_copula_mle(\n    CopulaClass,\n    u,\n    v,\n    bounds=(lower, upper),\n)\n\ntheta_hat = result.theta\n</code></pre>"},{"location":"concepts/mathematical_foundations/#why-pseudo-observations-must-stay-inside-the-open-unit-interval","title":"Why pseudo-observations must stay inside the open unit interval","text":"<p>A copula CDF is defined on</p> \\[ [0,1]^2. \\] <p>However, likelihood estimation uses the copula density and log-density.</p> <p>For numerical fitting, the estimator requires</p> \\[ 0&lt;u_i&lt;1, \\qquad 0&lt;v_i&lt;1. \\] <p>Rank-based pseudo-observations use</p> \\[ u_i=\\frac{R_i}{n+1}, \\] <p>which naturally avoids the exact upper boundary \\(1\\).</p>"},{"location":"concepts/mathematical_foundations/#copula-likelihood-and-pseudo-likelihood","title":"Copula likelihood and pseudo-likelihood","text":"<p>If the marginal distributions are known and \\(U,V\\) are exact transformed uniform variables, maximizing</p> \\[ \\sum_i\\log c_\\theta(u_i,v_i) \\] <p>is ordinary copula maximum likelihood.</p> <p>If the transformations are estimated from the same sample, especially through ranks, the same objective is commonly called a copula pseudo-likelihood.</p> <p>The current estimator maximizes the supplied copula log-likelihood without trying to determine which marginal-estimation strategy generated \\(u\\) and \\(v\\).</p>"},{"location":"concepts/mathematical_foundations/#disjoint-parameter-spaces","title":"Disjoint parameter spaces","text":"<p>A numerical optimizer normally searches a continuous bounded interval.</p> <p>Some copula families require more than one search interval.</p> <p>For Frank,</p> \\[ \\theta\\in\\mathbb R\\setminus\\{0\\}. \\] <p>The two regions</p> \\[ \\theta&lt;0 \\] <p>and</p> \\[ \\theta&gt;0 \\] <p>can therefore be searched independently.</p> <p>The estimator compares the optimized log-likelihoods from both regions and selects the better result.</p>"},{"location":"copulas/frank_copula/","title":"Frank copula","text":""},{"location":"copulas/frank_copula/#definition","title":"Definition","text":"<p>The bivariate Frank copula is</p> \\[ C_\\theta(u,v) = -\\frac{1}{\\theta} \\ln \\left( 1+ \\frac{ (e^{-\\theta u}-1)(e^{-\\theta v}-1) }{ e^{-\\theta}-1 } \\right), \\] <p>for</p> \\[ \\theta\\in\\mathbb R\\setminus\\{0\\}. \\] <p>Positive \\(\\theta\\) represents positive dependence and negative \\(\\theta\\) represents negative dependence.</p> <p>Independence is obtained as the limiting case</p> \\[ \\theta\\to0. \\]"},{"location":"copulas/frank_copula/#density","title":"Density","text":"<p>The copula density is</p> \\[ c_\\theta(u,v) = \\frac{ \\theta(1-e^{-\\theta})e^{-\\theta(u+v)} }{ \\left[ (1-e^{-\\theta}) - (1-e^{-\\theta u})(1-e^{-\\theta v}) \\right]^2 }. \\] <p>It is obtained from</p> \\[ c_\\theta(u,v) = \\frac{\\partial^2 C_\\theta(u,v)} {\\partial u\\,\\partial v}. \\]"},{"location":"copulas/frank_copula/#log-density","title":"Log-density","text":"<p>The direct log-density can be written as</p> \\[ \\log c_\\theta(u,v) = \\log\\left[ \\theta(1-e^{-\\theta}) \\right] -\\theta(u+v) - 2\\log \\left| (1-e^{-\\theta}) - (1-e^{-\\theta u})(1-e^{-\\theta v}) \\right|. \\] <p>Direct log-density evaluation is useful because parameter estimation works with sums of log-densities.</p>"},{"location":"copulas/frank_copula/#sample-log-likelihood","title":"Sample log-likelihood","text":"<p>For paired pseudo-observations</p> \\[ (u_1,v_1),\\ldots,(u_n,v_n), \\] <p>the Frank sample log-likelihood is</p> \\[ \\ell(\\theta) = \\sum_{i=1}^{n} \\log c_\\theta(u_i,v_i). \\] <p>The class exposes this through:</p> <pre><code>copula.log_likelihood(u, v)\n</code></pre> <p>when <code>copula</code> is a <code>FrankCopula</code> instantiated at a specific candidate \\(\\theta\\).</p>"},{"location":"copulas/frank_copula/#numerical-implementation","title":"Numerical implementation","text":"<p>The implementation uses NumPy operations such as:</p> <ul> <li><code>np.expm1(x)</code> for \\(e^x-1\\);</li> <li><code>np.log1p(x)</code> for \\(\\log(1+x)\\).</li> </ul> <p>These forms improve numerical accuracy near zero.</p>"},{"location":"copulas/frank_copula/#parameter-validation","title":"Parameter validation","text":"<p><code>theta=0</code> is rejected by the direct implementation because the formula contains \\(1/\\theta\\).</p> <p>The mathematical limit at zero corresponds to the independence copula but is not currently represented by <code>FrankCopula(theta=0)</code>.</p>"},{"location":"copulas/frank_copula/#parameter-estimation","title":"Parameter estimation","text":"<p>When \\(\\theta\\) is unknown, use the estimation layer.</p> <p>Because Frank excludes exactly zero, search the negative and positive parameter regions separately:</p> <pre><code>from pycopula_ml.copulas import FrankCopula\nfrom pycopula_ml.estimation import fit_copula_mle\n\n\nresult = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n\ntheta_hat = result.theta\n</code></pre> <p>Internally, each candidate value is evaluated through</p> \\[ -\\ell(\\theta), \\] <p>and the best successful interval is selected.</p> <p>The final result reports the statistical log-likelihood directly:</p> <pre><code>print(result.theta)\nprint(result.log_likelihood)\nprint(result.interval)\n</code></pre> <p>For Frank:</p> <ul> <li><code>result.theta &gt; 0</code> indicates positive dependence;</li> <li><code>result.theta &lt; 0</code> indicates negative dependence;</li> <li>an estimate numerically close to zero indicates approximate independence.</li> </ul>"},{"location":"copulas/frank_copula/#complete-workflow-from-original-data","title":"Complete workflow from original data","text":"<p>For raw paired measurements:</p> <pre><code>from pycopula_ml.copulas import FrankCopula\nfrom pycopula_ml.estimation import fit_copula_mle\nfrom pycopula_ml.marginals import bivariate_pseudo_observations\n\n\nu, v = bivariate_pseudo_observations(x, y)\n\nresult = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n\nfitted_copula = FrankCopula(theta=result.theta)\n</code></pre> <p>This separates:</p> <pre><code>x, y\n  \u2193\nmarginal/rank transformation\n  \u2193\nu, v\n  \u2193\nMLE\n  \u2193\ntheta_hat\n  \u2193\nfitted FrankCopula\n</code></pre>"},{"location":"copulas/frank_copula/#direct-evaluation-example","title":"Direct evaluation example","text":"<p>If \\(\\theta\\) is already known:</p> <pre><code>from pycopula_ml.copulas import FrankCopula\n\n\ncopula = FrankCopula(theta=2.0)\n\ncdf = copula.cdf(0.3, 0.7)\npdf = copula.pdf(0.3, 0.7)\nlogpdf = copula.logpdf(0.3, 0.7)\n</code></pre>"},{"location":"copulas/frank_copula/#mathematical-tests","title":"Mathematical tests","text":"<p>The test suite checks properties including:</p> \\[ C(u,0)=0, \\] \\[ C(0,v)=0, \\] \\[ C(u,1)=u, \\] \\[ C(1,v)=v, \\] <p>symmetry,</p> \\[ C(u,v)=C(v,u), \\] <p>positive density,</p> \\[ c(u,v)&gt;0, \\] <p>and consistency,</p> \\[ \\operatorname{logpdf}(u,v) = \\log(\\operatorname{pdf}(u,v)). \\] <p>The estimation layer is tested separately so failures in numerical fitting can be distinguished from failures in the Frank probability formulas.</p>"},{"location":"estimation/maximum_likelihood/","title":"Maximum-likelihood estimation","text":""},{"location":"estimation/maximum_likelihood/#public-interface","title":"Public interface","text":"<pre><code>from pycopula_ml.estimation import (\n    CopulaFitResult,\n    fit_copula_mle,\n)\n</code></pre> <p><code>fit_copula_mle()</code> estimates a scalar copula dependence parameter by maximizing the copula log-likelihood.</p>"},{"location":"estimation/maximum_likelihood/#basic-workflow","title":"Basic workflow","text":"<p>For original paired observations \\(x\\) and \\(y\\):</p> <pre><code>from pycopula_ml.copulas import FrankCopula\nfrom pycopula_ml.estimation import fit_copula_mle\nfrom pycopula_ml.marginals import bivariate_pseudo_observations\n\n\nu, v = bivariate_pseudo_observations(x, y)\n\nresult = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n\nprint(result.theta)\nprint(result.log_likelihood)\n</code></pre> <p>The workflow is</p> \\[ (x_i,y_i) \\rightarrow (u_i,v_i) \\rightarrow \\ell(\\theta) \\rightarrow \\widehat{\\theta}. \\]"},{"location":"estimation/maximum_likelihood/#objective-function","title":"Objective function","text":"<p>For every candidate parameter value, the estimator constructs</p> <pre><code>copula(theta=theta)\n</code></pre> <p>and evaluates</p> <pre><code>copula.log_likelihood(u, v)\n</code></pre> <p>which corresponds to</p> \\[ \\ell(\\theta) = \\sum_{i=1}^{n} \\log c_\\theta(u_i,v_i). \\] <p>SciPy's scalar optimizer minimizes</p> \\[ -\\ell(\\theta), \\] <p>and the public result converts the selected objective value back to the maximized log-likelihood.</p>"},{"location":"estimation/maximum_likelihood/#one-search-interval","title":"One search interval","text":"<p>For a family whose admissible parameter region can be represented by one bounded interval:</p> <pre><code>result = fit_copula_mle(\n    SomeCopula,\n    u,\n    v,\n    bounds=(0.01, 20.0),\n)\n</code></pre> <p>The lower bound must be strictly smaller than the upper bound.</p>"},{"location":"estimation/maximum_likelihood/#multiple-search-intervals","title":"Multiple search intervals","text":"<p>Disjoint domains can be represented by a sequence of intervals:</p> <pre><code>result = fit_copula_mle(\n    FrankCopula,\n    u,\n    v,\n    bounds=(\n        (-50.0, -1e-6),\n        (1e-6, 50.0),\n    ),\n)\n</code></pre> <p>The estimator:</p> <ol> <li>optimizes the negative log-likelihood on each interval;</li> <li>discards unsuccessful or non-finite solutions;</li> <li>compares the successful objective values;</li> <li>returns the solution with the largest log-likelihood.</li> </ol> <p>This removes Frank-specific branching from user scripts.</p>"},{"location":"estimation/maximum_likelihood/#result-object","title":"Result object","text":"<p>The return value is a <code>CopulaFitResult</code>.</p> <p>Important fields are:</p> <pre><code>result.theta\nresult.log_likelihood\nresult.success\nresult.message\nresult.nfev\nresult.n_obs\nresult.interval\nresult.raw_result\nresult.interval_results\n</code></pre>"},{"location":"estimation/maximum_likelihood/#theta","title":"<code>theta</code>","text":"<p>Maximum-likelihood estimate:</p> \\[ \\widehat{\\theta}. \\]"},{"location":"estimation/maximum_likelihood/#log_likelihood","title":"<code>log_likelihood</code>","text":"<p>Maximized value</p> \\[ \\ell(\\widehat{\\theta}). \\] <p>This is returned with the statistical sign convention, even though the underlying optimizer minimizes the negative log-likelihood.</p>"},{"location":"estimation/maximum_likelihood/#interval","title":"<code>interval</code>","text":"<p>The parameter interval that produced the selected optimum.</p> <p>For a Frank search over both signs, this makes it explicit whether the selected solution came from the negative or positive dependence region.</p>"},{"location":"estimation/maximum_likelihood/#interval_results","title":"<code>interval_results</code>","text":"<p>Raw SciPy results for every interval searched.</p> <p>This is mainly useful for diagnostics and development. Most user code should prefer the structured top-level fields.</p>"},{"location":"estimation/maximum_likelihood/#input-validation","title":"Input validation","text":"<p>The estimator requires <code>u</code> and <code>v</code> to be:</p> <ul> <li>one-dimensional;</li> <li>non-empty;</li> <li>equal in shape;</li> <li>finite;</li> <li>strictly inside \\((0,1)\\).</li> </ul> <p>For example, exact boundary values are rejected:</p> <pre><code>u = 0\nu = 1\nv = 0\nv = 1\n</code></pre> <p>This is stricter than the generic copula CDF domain \\([0,1]\\) because log-likelihood evaluation is an interior-density calculation.</p>"},{"location":"estimation/maximum_likelihood/#optimization-controls","title":"Optimization controls","text":"<p>The current function exposes:</p> <pre><code>xatol\nmaxiter\n</code></pre> <p><code>xatol</code> controls the absolute tolerance of the estimated scalar parameter.</p> <p><code>maxiter</code> controls the maximum number of optimizer iterations per search interval.</p> <p>The implementation currently uses SciPy's bounded scalar optimizer internally. This is intentionally hidden behind the <code>pycopula_ml.estimation</code> API.</p>"},{"location":"estimation/maximum_likelihood/#frank-example-and-association-direction","title":"Frank example and association direction","text":"<p>For the bivariate Frank copula:</p> \\[ \\widehat{\\theta}&gt;0 \\] <p>indicates positive dependence, while</p> \\[ \\widehat{\\theta}&lt;0 \\] <p>indicates negative dependence.</p> <p>A fitted value very close to zero indicates approximate independence, even though the direct Frank implementation excludes exactly</p> \\[ \\theta=0. \\] <p>Example:</p> <pre><code>import numpy as np\n\ntheta_hat = result.theta\n\nif np.isclose(theta_hat, 0.0, atol=1e-4):\n    association = \"approximately independent\"\nelif theta_hat &gt; 0.0:\n    association = \"positive\"\nelse:\n    association = \"negative\"\n</code></pre>"},{"location":"estimation/maximum_likelihood/#numerical-failures","title":"Numerical failures","text":"<p>If a candidate parameter produces a non-finite log-likelihood, the internal objective treats that candidate as invalid.</p> <p>If every searched interval fails to produce a finite successful optimization, <code>fit_copula_mle()</code> raises <code>RuntimeError</code>.</p> <p>This is preferable to silently returning a meaningless parameter estimate.</p>"},{"location":"estimation/maximum_likelihood/#design-note","title":"Design note","text":"<p>The user-facing code does not import <code>scipy.optimize</code>.</p> <p>That separation is intentional:</p> <pre><code>user code\n    \u2193\npycopula_ml.estimation.fit_copula_mle\n    \u2193\nSciPy optimization backend\n</code></pre> <p>The optimization backend can therefore change later without changing the public estimation workflow.</p>"},{"location":"getting-started/development/","title":"Development workflow","text":""},{"location":"getting-started/development/#environment","title":"Environment","text":"<p>Create a virtual environment:</p> <pre><code>python3 -m venv .env\nsource .env/bin/activate\n</code></pre> <p>Install in editable mode:</p> <pre><code>python -m pip install --upgrade pip\npython -m pip install -e \".[dev]\"\n</code></pre> <p>Prefer:</p> <pre><code>python -m pip\npython -m pytest\n</code></pre> <p>over bare <code>pip3</code> or <code>pytest</code> commands when multiple Python installations exist on the same machine.</p>"},{"location":"getting-started/development/#editable-installation","title":"Editable installation","text":"<p>The project uses a <code>src/</code> layout:</p> <pre><code>src/\n\u2514\u2500\u2500 pycopula_ml/\n    \u251c\u2500\u2500 copulas/\n    \u251c\u2500\u2500 estimation/\n    \u2514\u2500\u2500 marginals/\n</code></pre> <p>Editable installation ensures imports resolve through the active environment while source changes remain immediately available.</p>"},{"location":"getting-started/development/#style","title":"Style","text":"<p>The project uses Ruff for formatting and linting.</p> <pre><code>ruff format .\nruff check .\n</code></pre> <p>The current configuration targets PEP 8-compatible conventions and import ordering.</p>"},{"location":"getting-started/development/#test-before-commit","title":"Test before commit","text":"<p>Recommended sequence:</p> <pre><code>ruff format .\nruff check .\npython -m pytest\n</code></pre> <p>For the estimation module specifically:</p> <pre><code>python -m pytest tests/estimation -v\n</code></pre>"},{"location":"getting-started/development/#test-layers","title":"Test layers","text":"<p>The project distinguishes several testing responsibilities.</p>"},{"location":"getting-started/development/#unit-tests","title":"Unit tests","text":"<p>Unit tests are deterministic and belong in <code>tests/</code>.</p> <p>For estimation, they should test the optimizer independently from a specific copula family whenever possible.</p> <p>For example, a synthetic log-likelihood with a known maximum can verify that <code>fit_copula_mle()</code> correctly:</p> <ul> <li>finds the optimum;</li> <li>searches multiple intervals;</li> <li>validates input domains;</li> <li>rejects invalid bounds.</li> </ul>"},{"location":"getting-started/development/#integration-tests","title":"Integration tests","text":"<p>Integration tests verify that multiple package layers work together.</p> <p>A representative fitting path is:</p> <pre><code>bivariate_pseudo_observations()\n        \u2193\nFrankCopula\n        \u2193\nfit_copula_mle()\n</code></pre> <p>These tests should remain deterministic if they are included in continuous integration.</p>"},{"location":"getting-started/development/#statistical-validation-scripts","title":"Statistical validation scripts","text":"<p>Monte Carlo experiments should live under a path such as:</p> <pre><code>scripts/validation/\n</code></pre> <p>They are used to study statistical properties such as:</p> <ul> <li>estimator bias;</li> <li>MAE;</li> <li>RMSE;</li> <li>variability across repeated samples;</li> <li>effect of sample size.</li> </ul> <p>They should not replace deterministic unit tests.</p>"},{"location":"getting-started/development/#distribution-vs-import-name","title":"Distribution vs import name","text":"<p>The package distribution name is:</p> <pre><code>pycopula-ml\n</code></pre> <p>The Python import name is:</p> <pre><code>pycopula_ml\n</code></pre> <p>Example:</p> <pre><code>import pycopula_ml\n</code></pre>"},{"location":"getting-started/development/#current-dependencies","title":"Current dependencies","text":"<p>Core:</p> <ul> <li>NumPy</li> <li>SciPy</li> </ul> <p>Development:</p> <ul> <li>pytest</li> <li>pytest-cov</li> <li>Ruff</li> </ul> <p>Documentation dependencies may include:</p> <ul> <li>Sphinx</li> <li>numpydoc</li> </ul> <p>SciPy currently provides both:</p> <ul> <li>continuous distribution functionality used by marginal models;</li> <li>bounded scalar numerical optimization used internally by the estimation   layer.</li> </ul> <p>User code should normally access optimization through <code>pycopula_ml.estimation</code> rather than import <code>scipy.optimize</code> directly.</p>"},{"location":"getting-started/development/#public-apis","title":"Public APIs","text":"<p>Prefer imports from package <code>__init__.py</code> files.</p> <p>Recommended:</p> <pre><code>from pycopula_ml.copulas import FrankCopula\nfrom pycopula_ml.estimation import fit_copula_mle\nfrom pycopula_ml.marginals import MarginalSelector\n</code></pre> <p>rather than importing internal implementation paths unless needed for development.</p> <p>Recommended:</p> <pre><code>from pycopula_ml.estimation import fit_copula_mle\n</code></pre> <p>Internal-development import:</p> <pre><code>from pycopula_ml.estimation.maximum_likelihood import fit_copula_mle\n</code></pre> <p>The first form is preferred for user-facing code.</p>"},{"location":"getting-started/development/#internal-helpers","title":"Internal helpers","text":"<p>Functions and methods beginning with <code>_</code>, such as:</p> <pre><code>_prepare_inputs()\n_prepare_pseudo_observations()\n_negative_log_likelihood()\n</code></pre> <p>are internal implementation details.</p> <p>They are not considered stable public API.</p>"},{"location":"getting-started/development/#documentation-conventions","title":"Documentation conventions","text":"<p>Docstrings currently follow NumPy-style structure:</p> <pre><code>Parameters\n----------\n...\n\nReturns\n-------\n...\n\nRaises\n------\n...\n\nNotes\n-----\n...\n</code></pre> <p>Mathematical expressions are written with Sphinx-compatible math directives where appropriate.</p>"},{"location":"getting-started/development/#version-status","title":"Version status","text":"<p>Current development version:</p> <pre><code>0.1.0\n</code></pre> <p>Development status:</p> <pre><code>Alpha\n</code></pre> <p>The public API should not yet be considered stable.</p>"},{"location":"marginals/empirical_marginals/","title":"Empirical marginals","text":""},{"location":"marginals/empirical_marginals/#empirical-cdf","title":"Empirical CDF","text":"<p>For observations</p> \\[ X_1,\\ldots,X_n, \\] <p>the empirical cumulative distribution function is</p> \\[ \\widehat F_n(x) = \\frac{1}{n} \\sum_{i=1}^n \\mathbf 1(X_i\\leq x). \\] <p><code>EmpiricalMarginal</code> stores the sample and evaluates this step function efficiently using sorted observations and <code>numpy.searchsorted</code>.</p>"},{"location":"marginals/empirical_marginals/#example","title":"Example","text":"<pre><code>import numpy as np\n\nfrom pycopula_ml.marginals import EmpiricalMarginal\n\n\nx = np.array([10.0, 20.0, 30.0, 40.0])\n\nmarginal = EmpiricalMarginal().fit(x)\n\nprint(marginal.cdf([5.0, 10.0, 25.0, 40.0]))\n</code></pre> <p>Expected ECDF values are</p> \\[ [0,\\;0.25,\\;0.5,\\;1]. \\]"},{"location":"marginals/empirical_marginals/#pseudo-observations","title":"Pseudo-observations","text":"<p>For copula estimation, the package also provides rank-based pseudo-observations:</p> \\[ u_i = \\frac{R_i}{n+1}. \\] <p>The \\(n+1\\) denominator keeps the transformed observations away from the exact value \\(1\\).</p> <p>Example:</p> <pre><code>from pycopula_ml.marginals import pseudo_observations\n\nu = pseudo_observations(x)\n</code></pre>"},{"location":"marginals/empirical_marginals/#ties","title":"Ties","text":"<p>The default ranking method is average rank.</p> <p>For</p> \\[ [10,20,20,40], \\] <p>the two tied values receive the same average rank.</p> <p>Alternative ranking behavior can be requested through the <code>ties</code> argument where supported by <code>scipy.stats.rankdata</code>.</p>"},{"location":"marginals/empirical_marginals/#bivariate-pseudo-observations","title":"Bivariate pseudo-observations","text":"<pre><code>from pycopula_ml.marginals import (\n    bivariate_pseudo_observations,\n)\n\nu, v = bivariate_pseudo_observations(x, y)\n</code></pre> <p>Both samples must contain the same number of observations.</p> <p>Each marginal is ranked independently.</p>"},{"location":"marginals/empirical_marginals/#ecdf-vs-pseudo-observations","title":"ECDF vs pseudo-observations","text":"<p>These are related but not identical.</p> <p>For the largest fitted observation,</p> \\[ \\widehat F_n(x_{\\max})=1. \\] <p>Rank-based pseudo-observations instead yield</p> \\[ u_{\\max} = \\frac{n}{n+1} &lt;1. \\] <p>For copula pseudo-likelihood estimation, the rank transformation is usually the more direct tool.</p>"},{"location":"marginals/model_selection/","title":"Marginal model selection","text":""},{"location":"marginals/model_selection/#marginalselector","title":"<code>MarginalSelector</code>","text":"<p><code>MarginalSelector</code> fits several candidate parametric distributions and ranks successful fits.</p> <p>Default candidates currently include:</p> <ul> <li>Normal;</li> <li>Gamma;</li> <li>Lognormal;</li> <li>Weibull;</li> <li>Exponential.</li> </ul>"},{"location":"marginals/model_selection/#selection-criteria","title":"Selection criteria","text":"<p>Supported criteria:</p> <pre><code>aic\nbic\nks\n</code></pre> <p>The selected model minimizes the configured quantity.</p>"},{"location":"marginals/model_selection/#aic","title":"AIC","text":"\\[ AIC=2k-2\\ell. \\] <p>Lower is better.</p>"},{"location":"marginals/model_selection/#bic","title":"BIC","text":"\\[ BIC=k\\log n-2\\ell. \\] <p>Lower is better.</p>"},{"location":"marginals/model_selection/#ks-statistic","title":"KS statistic","text":"\\[ D = \\sup_x \\left| F_n(x)-F(x) \\right|. \\] <p>Lower is better.</p>"},{"location":"marginals/model_selection/#why-the-ks-p-value-is-not-the-default-selector","title":"Why the KS p-value is not the default selector","text":"<p>The classical one-sample KS p-value assumes a fully specified theoretical distribution.</p> <p>When the same sample is used both to estimate distribution parameters and to compute the KS test, the standard p-value is no longer formally calibrated in the usual way.</p> <p>The package therefore stores it as an exploratory diagnostic rather than using it as the default selection rule.</p>"},{"location":"marginals/model_selection/#selected-does-not-mean-true","title":"Selected does not mean true","text":"<p>The package uses the name</p> <pre><code>selected_model_\n</code></pre> <p>rather than interpreting the model as the true generating distribution.</p> <p>The meaning is:</p> <p>preferred model among the supplied candidate set under the configured selection criterion.</p> <p>Aliases such as <code>best_model_</code> may exist for convenience, but <code>selected_model_</code> is statistically more precise.</p>"},{"location":"marginals/model_selection/#example","title":"Example","text":"<pre><code>from pycopula_ml.marginals import MarginalSelector\n\n\nselector = MarginalSelector(\n    criterion=\"aic\",\n)\n\nselector.fit(x)\n\nprint(selector.summary())\n\nmodel = selector.selected_model_\nu = selector.transform(x)\n</code></pre>"},{"location":"marginals/model_selection/#custom-candidates","title":"Custom candidates","text":"<pre><code>from scipy.stats import gamma, lognorm, norm\n\nfrom pycopula_ml.marginals import (\n    MarginalCandidate,\n    MarginalSelector,\n)\n\n\ncandidates = [\n    MarginalCandidate(\n        name=\"normal\",\n        distribution=norm,\n    ),\n    MarginalCandidate(\n        name=\"gamma\",\n        distribution=gamma,\n        fit_kwargs={\"floc\": 0.0},\n    ),\n    MarginalCandidate(\n        name=\"lognormal\",\n        distribution=lognorm,\n        fit_kwargs={\"floc\": 0.0},\n    ),\n]\n\nselector = MarginalSelector(\n    candidates=candidates,\n    criterion=\"aic\",\n)\n</code></pre>"},{"location":"marginals/model_selection/#warnings","title":"Warnings","text":"<p>The selector can warn about:</p> <ul> <li>small samples;</li> <li>fitted support boundaries close to observed extrema;</li> <li>candidate fitting failures.</li> </ul> <p>These warnings are diagnostic. They do not automatically invalidate a model.</p>"},{"location":"marginals/model_selection/#small-samples","title":"Small samples","text":"<p>Automatic distribution selection is especially unstable when the sample is very small relative to the number of fitted parameters.</p> <p>The package should therefore report rather than hide this uncertainty.</p>"},{"location":"marginals/model_selection/#future-improvements","title":"Future improvements","text":"<p>Planned selection improvements include:</p> <ul> <li>AICc;</li> <li>goodness-of-fit bootstrap;</li> <li>diagnostic plots;</li> <li>transformed-uniform diagnostics;</li> <li>cross-validation where appropriate;</li> <li>automatic support-aware candidate filtering.</li> </ul>"},{"location":"marginals/parametric_marginals/","title":"Parametric marginals","text":""},{"location":"marginals/parametric_marginals/#purpose","title":"Purpose","text":"<p><code>ParametricMarginal</code> wraps continuous distributions from <code>scipy.stats</code>.</p> <p>The package does not reimplement Normal, Gamma, Lognormal, Weibull, or Exponential distributions.</p> <p>SciPy remains responsible for:</p> <ul> <li>parameter fitting;</li> <li>PDF evaluation;</li> <li>log-PDF evaluation;</li> <li>CDF evaluation;</li> <li>distribution support.</li> </ul> <p><code>pycopula-ml</code> adds a consistent interface and diagnostics needed by the copula workflow.</p>"},{"location":"marginals/parametric_marginals/#example-normal","title":"Example: Normal","text":"<pre><code>from scipy.stats import norm\n\nfrom pycopula_ml.marginals import ParametricMarginal\n\n\nmarginal = ParametricMarginal(\n    norm,\n    name=\"normal\",\n)\n\nmarginal.fit(x)\n\nu = marginal.transform(x)\n</code></pre> <p>For a Normal model, SciPy uses</p> \\[ \\mu=\\text{loc}, \\qquad \\sigma=\\text{scale}. \\]"},{"location":"marginals/parametric_marginals/#example-gamma-with-fixed-location","title":"Example: Gamma with fixed location","text":"<pre><code>from scipy.stats import gamma\n\nfrom pycopula_ml.marginals import ParametricMarginal\n\n\nmarginal = ParametricMarginal(\n    gamma,\n    name=\"gamma\",\n    fit_kwargs={\n        \"floc\": 0.0,\n    },\n)\n\nmarginal.fit(x)\n</code></pre> <p>Fixing <code>loc=0</code> is a modeling assumption and should only be used when the variable's support justifies it.</p>"},{"location":"marginals/parametric_marginals/#fit-result","title":"Fit result","text":"<p>A successful fit stores a <code>MarginalFitResult</code> containing:</p> <ul> <li>distribution name;</li> <li>fitted parameters;</li> <li>sample size;</li> <li>number of free parameters;</li> <li>log-likelihood;</li> <li>AIC;</li> <li>BIC;</li> <li>KS statistic;</li> <li>exploratory KS p-value.</li> </ul>"},{"location":"marginals/parametric_marginals/#information-criteria","title":"Information criteria","text":"<p>For \\(k\\) free parameters,</p> \\[ AIC = 2k-2\\ell, \\] <p>and</p> \\[ BIC = k\\log n-2\\ell. \\] <p>Lower values are preferred within the candidate set.</p> <p>Fixed parameters are excluded from the free-parameter count when possible.</p>"},{"location":"marginals/parametric_marginals/#probability-integral-transform","title":"Probability integral transform","text":"<p>After fitting,</p> <pre><code>u = marginal.transform(x)\n</code></pre> <p>computes</p> \\[ u_i=\\widehat F_X(x_i) \\] <p>and clips numerical boundaries into the open unit interval.</p>"},{"location":"references/references/","title":"References","text":""},{"location":"references/references/#copulas-and-bivariate-distributions","title":"Copulas and bivariate distributions","text":"<p>Balakrishnan, N., &amp; Lai, C.-D. (2009). Continuous Bivariate Distributions, Second Edition. Springer.</p> <p>Relevant topics include:</p> <ul> <li>bivariate copulas;</li> <li>continuous bivariate distributions;</li> <li>Frank distribution/copula;</li> <li>dependence modeling.</li> </ul>"},{"location":"references/references/#copula-graphical-neural-networks","title":"Copula graphical neural networks","text":"<p>The project also uses the CopulaGNN literature as broader context for combining copulas, dependence modeling, and graph-based machine learning.</p>"},{"location":"references/references/#mlcopula","title":"MLCOPULA","text":"<p>The semester project is motivated by the R package MLCOPULA and the objective of reproducing and modernizing its copula-based classification workflow in Python.</p> <p>As implementation parity progresses, this documentation should record:</p> <ul> <li>R source function;</li> <li>corresponding Python component;</li> <li>whether behavior is directly reproduced or intentionally redesigned;</li> <li>numerical differences;</li> <li>test fixtures;</li> <li>version of the R package used as reference.</li> </ul>"},{"location":"references/references/#scipy","title":"SciPy","text":"<p>SciPy provides the numerical and statistical backend used by several parts of the package.</p> <p>Marginal modeling relies on functionality such as:</p> <ul> <li><code>distribution.fit</code>;</li> <li><code>distribution.pdf</code>;</li> <li><code>distribution.logpdf</code>;</li> <li><code>distribution.cdf</code>;</li> <li><code>distribution.support</code>;</li> <li><code>scipy.stats.kstest</code>;</li> <li><code>scipy.stats.rankdata</code>.</li> </ul> <p>The parameter-estimation layer currently relies internally on:</p> <ul> <li><code>scipy.optimize.minimize_scalar</code>.</li> </ul> <p>The public package API intentionally wraps these capabilities instead of exposing SciPy implementation details as part of the main user workflow.</p>"},{"location":"references/references/#estimation-terminology","title":"Estimation terminology","text":"<p>For copula fitting, the package documentation distinguishes between:</p> <ul> <li>likelihood based on exact transformed uniforms under known marginals; and</li> <li>pseudo-likelihood based on estimated or rank-derived pseudo-observations.</li> </ul> <p>The numerical objective has the same computational form,</p> \\[ \\ell(\\theta) = \\sum_i \\log c_\\theta(u_i,v_i), \\] <p>but the statistical interpretation depends on how \\(u_i,v_i\\) were obtained.</p>"},{"location":"testing/estimation_validation/","title":"Estimation testing and statistical validation","text":"<p>Parameter estimation requires more than one kind of test.</p> <p>A deterministic software test and a Monte Carlo validation experiment answer different questions and should not be mixed.</p>"},{"location":"testing/estimation_validation/#unit-tests","title":"Unit tests","text":"<p>The estimation unit tests verify the behavior of the optimization code itself.</p> <p>A useful deterministic test defines a synthetic log-likelihood with a known maximum, for example</p> \\[ \\ell(\\theta) = -(\\theta-2.5)^2. \\] <p>Its exact optimum is</p> \\[ \\widehat{\\theta}=2.5. \\] <p>The optimizer can therefore be tested without depending on the Frank implementation.</p> <p>Current estimation unit tests should cover:</p> <ul> <li>recovery of a known deterministic maximum;</li> <li>selection of the best result across disjoint intervals;</li> <li>rejection of values outside \\((0,1)\\);</li> <li>rejection of mismatched sample shapes;</li> <li>rejection of reversed parameter bounds.</li> </ul> <p>Run them with:</p> <pre><code>python -m pytest tests/estimation -v\n</code></pre> <p>These tests belong in <code>tests/</code> because they are deterministic, fast, and suitable for continuous integration.</p>"},{"location":"testing/estimation_validation/#integration-tests","title":"Integration tests","text":"<p>An integration test answers a broader question:</p> <p>Do the marginal transformation, copula implementation, and estimation layer work correctly together?</p> <p>A representative integration path is:</p> <pre><code>x, y\n  \u2193\nbivariate_pseudo_observations()\n  \u2193\nu, v\n  \u2193\nFrankCopula\n  \u2193\nfit_copula_mle()\n  \u2193\ntheta_hat\n</code></pre> <p>Integration tests should use fixed data and deterministic expected behavior so they remain stable in CI.</p> <p>At the current stage, this is the recommended next testing layer after the estimation unit tests.</p>"},{"location":"testing/estimation_validation/#statistical-validation","title":"Statistical validation","text":"<p>A Monte Carlo experiment answers a statistical question rather than a software unit-test question.</p> <p>For a known generating parameter,</p> \\[ \\theta_{\\text{true}}, \\] <p>generate repeated samples</p> \\[ (U_i,V_i) \\sim C_{\\text{Frank}}(\\theta_{\\text{true}}) \\] <p>and estimate</p> \\[ \\widehat{\\theta} \\] <p>for each sample.</p> <p>The validation script can then study the sampling behavior of the estimator.</p> <p>This belongs under a path such as:</p> <pre><code>scripts/validation/\n</code></pre> <p>rather than the core unit-test suite.</p>"},{"location":"testing/estimation_validation/#bias","title":"Bias","text":"<p>For \\(R\\) repeated experiments,</p> \\[ \\operatorname{Bias} = \\frac{1}{R} \\sum_{r=1}^{R} (\\widehat{\\theta}_r-\\theta_{\\text{true}}). \\] <p>Bias measures systematic directional error.</p> <ul> <li>positive bias: tendency to overestimate;</li> <li>negative bias: tendency to underestimate;</li> <li>bias near zero: no strong systematic direction in the experiment.</li> </ul>"},{"location":"testing/estimation_validation/#mae","title":"MAE","text":"<p>The mean absolute error is</p> \\[ MAE = \\frac{1}{R} \\sum_{r=1}^{R} \\left| \\widehat{\\theta}_r-\\theta_{\\text{true}} \\right|. \\] <p>It measures the average magnitude of the estimation error.</p> <p>Unlike bias, positive and negative errors do not cancel.</p>"},{"location":"testing/estimation_validation/#rmse","title":"RMSE","text":"<p>The root mean squared error is</p> \\[ RMSE = \\sqrt{ \\frac{1}{R} \\sum_{r=1}^{R} (\\widehat{\\theta}_r-\\theta_{\\text{true}})^2 }. \\] <p>RMSE gives greater weight to unusually large errors than MAE.</p> <p>Typically,</p> \\[ RMSE\\geq MAE. \\]"},{"location":"testing/estimation_validation/#recommended-validation-grid","title":"Recommended validation grid","text":"<p>A stronger statistical validation should vary both dependence strength and sample size.</p> <p>For example:</p> <pre><code>theta_true = 1, 2, 5, 10\nn          = 25, 50, 100, 250, 500, 1000\n</code></pre> <p>For each configuration, repeat the experiment many times and record:</p> <ul> <li>mean \\(\\widehat{\\theta}\\);</li> <li>standard deviation of \\(\\widehat{\\theta}\\);</li> <li>bias;</li> <li>MAE;</li> <li>RMSE;</li> <li>optimizer success rate.</li> </ul> <p>The expected large-sample behavior is that estimation error decreases as the sample size grows.</p> <p>This is evidence about the statistical estimator. It is not a replacement for deterministic unit tests.</p>"}]}